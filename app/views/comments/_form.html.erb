<%= form_with model: [ @meme, @meme.comments.build ] do |f| %>
  <p class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p class ="actions">
    <%= f.submit %>
  </p>
<% end %>



